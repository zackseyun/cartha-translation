# Targeted Jubilees chapter refinements

These files capture chapter-specific Vertex extraction repairs for chapters that
still looked obviously weak after the broader hybrid corpus build.

Current targeted repairs:

- `ch23_vertex_refined.json`
- `ch39_vertex_refined.json`

Both were produced with explicit running-head constraints so the model could
separate the target chapter from the neighboring chapter material on the same
PDF pages.
