፡ was ich sah; ce que je vis`
            *   `εχων ἁ ηκουσα εγω — om ως — `
            *   Wait, is it `ἁ` or `α`? It looks like `ἁ` with a rough breathing. Let me zoom in. Yes, `ἁ`.
            *   `ዘእ` -> `ዘ` (za), `እ` (e), `ኜ` (ñe)? No, it looks like `ሬ` (re) or `ጼ` (tse) or `ኜ` (ñe). Let's look closer. It's `ዘእሬኢ`. `ሬ` is U+12ሬ. Wait, `ሬ` is `ረ` + `ኤ`. Let's check the image. It looks like `እ` followed by `ሬ` followed by `ኢ`. `ዘእሬኢ`. Let me re-examine. It looks like `ዘእ` then a character that looks like `ኜ` or `ጼ`. Let's look at Ethiopic script. It's `ዘእሬኢ` (za-'e-re-'i). Let's check the shape of `ሬ`. Yes, it has a loop at the bottom right. The image has a character with a loop