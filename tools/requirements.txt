# Runtime dependencies for the Cartha Open Bible translation pipeline.
# Install with: pip install -r tools/requirements.txt

anthropic>=0.39.0          # Claude API client
openai>=1.54.0             # GPT cross-check
google-genai>=0.3.0        # Gemini cross-check (new unified SDK)
pyyaml>=6.0                # YAML output for verse records
jsonschema>=4.22.0         # Validation against verse.schema.json
cryptography>=42.0         # ed25519 signing (tools/sign.py)
python-dotenv>=1.0         # Optional .env loading for local dev
